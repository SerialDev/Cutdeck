[project]
name = "cutdeck"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "daggyd",
    "fastapi",
    "uvicorn[standard]",
]

[tool.uv.sources]
daggyd = { git = "https://github.com/SerialDev/DaggyD.git" }
